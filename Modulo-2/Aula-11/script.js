document.querySelector('#titulo').innerHTML = 'Mudando o titulosssss';

document.querySelectorAll('.lista');

// Colocar esses comandos no console para consulta o resultado

//QUERYSELECTOR seleciona o primeiro elemento correspondido ou seja o primeiro elemento passado pelo usuario 

//QUERYSELECTORALL seleciona todos os documentos do criterio passado pelo usuario 